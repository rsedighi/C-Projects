#include <stdio.h>

int main()
{
    int i;
    int max = 10;
    
    while(max <= 10 && max >=2)
    {
        for(i = 1; i < max; i++)
        {
            printf("%d", i);
        }
        printf("\n");
        max--;
    }       
    return(0);
}
